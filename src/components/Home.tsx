import React from "react";

const HomePage = () => {
  return (
    <div className=" w-full h-[calc(100vh-4rem)] bg-bgPic bg-no-repeat bg-cover lg:bg-contain bg-center">
      HomePage
    </div>
  );
};

export default HomePage;
